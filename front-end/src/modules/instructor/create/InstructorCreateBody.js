import React from "react";
const InstructorCreateBody = ({ children }) => {
  return (
    <section className="flex flex-col items-center gap-8 pt-16">
      {children}
    </section>
  );
};

export default InstructorCreateBody;
